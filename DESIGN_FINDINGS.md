# ملاحظات معاينة ALC

- المستودع عبارة عن موقع HTML/CSS/JavaScript ثابت، ولا يحتوي على package.json.
- الصفحة الرئيسية الحالية تركز على روابط التواصل الاجتماعي، لكنها تبدأ بشعار كبير جدًا ومساحة علوية واسعة، ما يؤخر الوصول إلى الدعوة الرئيسية.
- التصميم الحالي يستخدم ألوانًا زرقاء وصفراء متدرجة مع بطاقات بيضاء وحواف مستديرة.
- بطاقات التواصل كثيرة ومتساوية بصريًا، ولا توجد دعوة رئيسية واضحة للتسجيل أو الاستفسار في بداية الصفحة.
- يوجد نموذج تسجيل داخل نافذة منبثقة، ويُرسل الطلب إلى Firebase ثم يفتح واتساب؛ يجب الحفاظ على هذه الوظيفة.
- يجب إبقاء روابط إنستغرام وفيسبوك وواتساب والقناة والموقع والهاتف والعروض والشهادات و"هويتنا التعليمية".
- التحسين المقترح: إعادة بناء الصفحة الرئيسية كواجهة عربية RTL حديثة ببطولة واضحة، شريط تنقل، CTA أساسي للتسجيل، CTA ثانوي لواتساب، شريط ثقة/إحصاءات، بطاقات برامج أو نقاط قيمة، ثم روابط التواصل والخدمات.
- تم إنشاء نسخة التطوير محليًا في `/home/ubuntu/ALC-development` على الفرع `development/landing-page-refresh`، بينما بقي مستودع ALC الأصلي منفصلًا دون تعديل.

## اتجاه التصميم المختار

هوية تعليمية احترافية وهادئة: كحلي عميق مع أزرق ملكي ولمسات صفراء مستوحاة من الشعار، خلفيات فاتحة دافئة، شبكة بطاقات غير متماثلة قليلًا، وتباعد واسع. سيتم استخدام خط عربي من Google Fonts مع حركة دخول خفيفة تحترم `prefers-reduced-motion`.

## نتائج الاختبار الأولي

- تم تحميل الصفحة الجديدة بنجاح عبر خادم محلي، وظهر العنوان الجديد، التنقل، زر التسجيل، قسم البرامج، قسم التواصل والتذييل.
- المعاينة البصرية أظهرت تسلسلًا هرميًا أوضح من النسخة الأصلية: الرسالة الرئيسية والدعوة للتسجيل تظهران مباشرة في القسم البطولي.
- لم يظهر أي إخراج في سجل المتصفح، ما يشير إلى عدم وجود أخطاء JavaScript ظاهرة أثناء التحميل الأولي.
- تم الحفاظ على نموذج التسجيل وتكامل Firebase وفتح محادثة واتساب بعد الإرسال.

## اختبار التفاعل

تم فتح نموذج التسجيل من زر الدعوة الرئيسية بنجاح، وظهر النموذج بتخطيط منظم وحقول قابلة للاستخدام. كما نجح زر الإغلاق وإعادة الصفحة إلى حالتها الطبيعية. لم يتم إرسال بيانات تجريبية فعلية حتى لا تُنشأ سجلات غير حقيقية في Firebase أو تُرسل رسالة واتساب.

تمت إعادة تحميل الصفحة بعد إصلاح وجهة روابط التسجيل إلى `#register`، ثم اختبار زر الدعوة الرئيسية؛ فُتحت النافذة المنبثقة بنجاح وما زال التفاعل يعمل.

نجح اختبار الإغلاق النهائي للنموذج، كما بقي سجل المتصفح خاليًا من الأخطاء بعد إعادة التحميل والفتح والإغلاق. جرى التحقق أيضًا من أن ملفات الشعار والأيقونات والصفحات المرتبطة تُخدم محليًا بحالة HTTP 200، وأن `git diff --check` لم يجد مسافات أو أخطاء تنسيق.
