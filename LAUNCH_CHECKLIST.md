# قائمة مراجعة إطلاق ALC.pro

## ما تم التحقق منه

- الصفحة الرئيسية الجديدة تعمل على خادم محلي وتعرض الهوية العربية RTL بصورة متجاوبة.
- قسم البطولة يحتوي على رسالة واضحة ودعوتين لاتخاذ إجراء: التسجيل وواتساب.
- روابط الأقسام الداخلية تعمل عبر `#about` و`#programs` و`#contact` و`#register`.
- نموذج التسجيل يفتح ويغلق بنجاح، ويحافظ على إرسال الطلب إلى Firebase وفتح رسالة واتساب بعد نجاح الإرسال.
- أصول الشعار والأيقونات والصفحات المرتبطة موجودة وتُخدم محليًا بحالة HTTP 200.
- لم تظهر أخطاء JavaScript في سجل المتصفح أثناء التحميل والاختبار اليدوي.
- اجتاز التغيير فحص `git diff --check`.

## قبل النشر الفعلي

- مراجعة نصوص البرامج وأي ادعاءات تسويقية مع إدارة المركز.
- تأكيد أن بيانات الهاتف والموقع وروابط الحسابات الاجتماعية ما زالت صحيحة.
- اختبار إرسال نموذج فعلي ببيانات حقيقية بعد التأكد من بيئة Firebase المناسبة.
- اختبار الصفحة على هاتف فعلي ومتصفحات إضافية، ثم نشر الفرع `ALC.pro` أو دمجه بعد الموافقة.
